import 'package:flutter/material.dart';

class MarketHomeTabPage extends StatelessWidget {
  const MarketHomeTabPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('İşletmeler Listesi')));
  }
}
